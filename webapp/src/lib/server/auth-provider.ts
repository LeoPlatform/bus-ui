/**
 * Optional custom auth provider (e.g. DscoAuthProvider).
 * For OSS, this file is a placeholder; loadAuthProvider() falls back to OAuthAuthProvider.
 * For Dsco deploy, replace this file with a default export of your provider class (gitignored).
 */
export default undefined;
